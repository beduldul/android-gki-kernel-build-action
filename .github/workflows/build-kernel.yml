name: Build GKI Kernel

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    name: Build Kernel
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Setup Build Environment
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential libncurses-dev bison flex libssl-dev libelf-dev bc git curl zip unzipper

      - name: Download AOSP Clang
        run: |
          mkdir -p clang
          curl -sSL https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86/+archive/refs/heads/main/clang-r488909.tar.gz | tar -xz -C clang

      - name: Build Kernel Binary
        run: |
          export PATH="$(pwd)/clang/bin:$PATH"
          make ARCH=arm64 O=out marble_defconfig
          make ARCH=arm64 O=out -j$(nproc) LLVM=1 LLVM_IAS=1

      - name: Upload Kernel Artifact
        uses: actions/upload-artifact@v4
        with:
          name: GKI-Kernel-Image
          path: out/arch/arm64/boot/Image
